#include "listorderdetails.h"

namespace Models {

ListOrderDetails::ListOrderDetails()
{

}

void ListOrderDetails::populate(QString name, QString amount, QString cost, QString total, QString tax)
{
  //m_data.push_back({order->Name->setvalue(name),order->Amount->setvalue(amount),order->cost->setvalue(cost),order->Total->setvalue(total),order->tax->setvalue(tax)});
}
}
